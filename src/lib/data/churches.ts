import type { ChurchRecord } from "@/lib/types/directory";

// Real launch data should be imported through Firebase, not reseeded from
// placeholder local records.
export const seedChurches: ChurchRecord[] = [];
